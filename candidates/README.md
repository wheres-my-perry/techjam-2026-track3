# Candidates

This directory contains unpromoted optimization candidates. They are retained
for reproducible ablations and are never imported by `main.py`.

The current V19 family lives in `v19/`. Its implementation aliases remain
available through `python -m tools.matrix_runner`, but the active submission is
still the root-level `v16_1_clean.py` artifact.
