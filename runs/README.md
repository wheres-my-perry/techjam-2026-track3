# Local runs

This directory is the gitignored workspace for generated artifacts:

- `benchmarks/`
- `profiles/`
- `traces/`
- `tmp/`
- `remote/`

Promote only reviewed, reproducible evidence into `results/`.
